---
name: geminion
description: Enable gemini auto-mode — every coding request is automatically delegated to Gemini.
user-invocable: true
allowed-tools:
  - bash
---

Run: `mkdir -p ~/.local/share && touch ~/.local/share/gemini-auto.flag`

Then reply: "Auto-gemini ON — every coding request will be delegated to Gemini automatically."
